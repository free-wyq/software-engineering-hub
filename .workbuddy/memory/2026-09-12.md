# 2026-09-12

## 集成 gitlab-release-train 到 se-hub
- 来源：`D:\richinfo\AINative\.agents\skills\gitlab-release-train`（内网 GitLab 发布分支全流程技能，三分支模型 cxy-master/cxy-dev/release，含 3 个纯标准库 Python 脚本）。
- 方式：整体搬入 `se-hub/references/delivery/gitlab-release-train/`（SKILL.md + scripts/*.py，不含 __pycache__/scan_out.txt），新建 `references/delivery/SKILL.md` 作为交付子 hub（复用 coding 的子 hub 模式），保持 se-hub 唯一注册入口（all-in-one）。
- 同步更新 `se-hub/SKILL.md` 路由表（交付关增加分支/发版/release 关键词）与 `README.md` 目录结构、状态。
- 注意：该技能凭证仅在 agent 记忆中（不落技能目录）；脚本默认内网 GitLab 地址/host 可经 `--host` 覆盖。

## 调整：gitlab-release-train 从 delivery 移至 integration
- 用户定位：该技能主轴是出包班车（切 release/提测合并/出包），发生在编码之后、测试之前，不应归交付。
- 新增 references/integration/（集成出包阶段，编码后测试前），gitlab-release-train 挂其下；delivery 恢复 .gitkeep 占位，留待部署/CI-CD/回滚。
- se-hub 路由表变为六阶段：需求/设计/编码/集成出包/测试/交付；README 同步更新。

## 交付阶段定位明确
- 用户定义：交付 = 项目经理视角的交付管理（发布计划/发布评审/上线协调/风险与回滚决策/发布清单/干系人通知/复盘），不是纯技术部署。
- 已更新 se-hub 路由表交付行与 README 说明；delivery 仍为 .gitkeep 占位待填充。

## 分支模型图迭代（gitlab-release-train SKILL.md）
- 用户多次修正后定稿：cxy-master 为根 → 挂 release 与 feat/fix → feat/fix 合入验证到 cxy-dev；合流箭头标注（↑合入发布 / ↓合入验证）。
- 关键语义：cxy-dev 即 dev、cxy-master 即 master，二者永远平行永不相交；dev 内容只随 feat/fix 走 release 进生产，不直接回流 master。已在图中文字补两行说明。

## Commit 3d64f7c
- 集成 gitlab-release-train 全部变更已提交(六阶段路由/新 description/delivery 定位 PM/分支模型图修正/README)。未 push。

## 调整为通用类型
- 已将 se-hub 从仓库 .claude/skills/ 复制到用户级 C:\Users\11607\.claude\skills\se-hub，跨项目生效；仓库保持唯一源（master），后续更新以仓库为准再同步。README 已含分发与通用性说明（未提交）。

## 定位澄清：技能开发库（通用类型）
- 用户纠正：本仓库定位是开发技能的通用库（在这里开发/维护软件工程技能），不是使用技能；此前误将 se-hub 复制到用户级 ~/.claude/skills/，已删除撤销。
- README 通用性说明已改写为技能开发库定位。

## 目录结构通用化
- 移除仓库内 .claude/ 安装位，se-hub 移至顶层 skills/se-hub；仓库定位纯技能开发库，消费者自行复制到各自技能目录。README 分发/通用性说明与目录结构同步更新。

## 目录再精简
- 用户指出 skills/ 中间层多余（se-hub 本身就是技能），已将 se-hub 提至仓库根目录（仓库即技能），删除 skills/。README 安装说明与目录结构同步，commit 8e0324e。
